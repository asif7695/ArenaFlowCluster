# Copy to .env.local to override defaults.
# Backend base URL the dashboard polls:
NEXT_PUBLIC_API_BASE=http://localhost:5000
# Poll interval in ms:
NEXT_PUBLIC_POLL_MS=2000
# Force the built-in client-side mock (run the UI with no backend):
# NEXT_PUBLIC_USE_MOCK=1
